<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/textColorWhite">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar_anuncios_organizacao"
        android:layout_width="0dp"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/tabLayoutBottomColor"
        android:elevation="4dp"
        android:textAlignment="center"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:popupTheme="@style/AppTheme"
        app:titleTextColor="@color/textColorWhite"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler_view_anuncios_organizacao"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/textColorWhite"
        app:layout_constraintTop_toBottomOf="@id/toolbar_anuncios_organizacao" />

</android.support.constraint.ConstraintLayout>